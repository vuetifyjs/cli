# @vuetify/mcp-cli

Standalone CLI for configuring the Vuetify MCP server in supported IDEs.

## Usage
Run:

```bash
vuetify-mcp install
```

Or configure hosted mode:

```bash
vuetify-mcp install --remote
```

The same functionality is available via:

```bash
vuetify mcp ...
```
